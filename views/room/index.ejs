<%- include('../layout/header.ejs') %>

    <!-- <br><br>
    <a href="/room/create">create</a>
    <% rooms.forEach(element=>{ %>
         <p>
             <a href="room/show/<%= element.id %>">
                 <p>created by: <%= element.user.dataValues.name  %> </p>
                 <p>room topic: <%= element.topic.dataValues.name %></p>
                <%= element.title %> ---  <%= element.descreption %>
             </a>

             <% if (user != null){ %>
                <% if (element.userId == user.id){ %>
                <a href="room/edit/<%= element.id %>">edit</a>
                <% } %>
            <% } %>
            </p>
    <%   })  %> -->

    <div class="w-10/12 mx-auto mt-4 flex justify-between flex-wrap">
        <div class="w-2/12 ">
            <%- include('../component/topic.ejs') %>
        </div>
        <div class="w-7/12 pl-5 text-xl">
            <div class="flex items-center justify-between">
                <div class="font-bold">
                    <p class="uppercase text-white">study room</p> 
                    <span class="text-sm mainColorText"><%= rooms.length %> Rooms available</span>
                </div>
                <div>
                    <a href="/room/create" class="bg-cyan-500 px-4 py-2 rounded text-gray-700"><i class="fas fa-plus"></i> Create Room</a>
                </div>
            </div>
            <%- include('../component/room.ejs') %>
        </div>
        <div class="w-3/12 pl-5">
            <!-- {% include 'base/components/message.html'  %} -->
         </div>
    </div>
    

<%- include('../layout/footer.ejs') %>